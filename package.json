{
  "name": "ww-api",
  "version": "1.10.1",
  "description": "HERE API proxy for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --outdir dist --var SENTRY_RELEASE:$(sentry-cli releases propose-version) && pnpm run sentry:sourcemaps",
    "deploy:dev": "wrangler deploy --env dev --outdir dist --var SENTRY_RELEASE:$(sentry-cli releases propose-version) && pnpm run sentry:sourcemaps",
    "sentry:sourcemaps": "sentry-cli releases new $(sentry-cli releases propose-version) && sentry-cli sourcemaps upload --release=$(sentry-cli releases propose-version) --strip-prefix 'dist/..' dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^2.9.1",
    "@peculiar/x509": "^2.0.0",
    "@sentry/cloudflare": "^8.55.1",
    "ai": "^6.0.208",
    "cbor2": "^2.3.0",
    "hono": "^4.12.12",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "@sentry/cli": "^3.6.0",
    "typescript": "7.0.1-rc",
    "vitest": "^4.1.9",
    "wrangler": "^4.103.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@sentry/cli"
    ]
  }
}
